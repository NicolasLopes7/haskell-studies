main = do
    let batata = "salve salve fml"

    putStrLn batata
    putStr ("eae" ++ batata)
    -- ⬆️ concatenate needs brackets
    putStr (show 36)
    -- ⬆️ print numbers needs show function to convert the number to a string